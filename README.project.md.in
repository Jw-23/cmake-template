# @PROJECT_NAME@

一个使用 CMake 与 CPM.cmake 的现代 C++ 项目。

## 初始化配置

| 配置 | 值 |
| --- | --- |
| C++ 标准 | C++@CPP_STANDARD@ |
| Google Test | @GTEST_ENABLED@ |
| Google Benchmark | @BENCHMARK_ENABLED@ |
| spdlog | @SPDLOG_ENABLED@ |
| fmt | @FMT_ENABLED@ |
| Eigen | @EIGEN_ENABLED@ |

这些配置是默认值，仍可在配置阶段通过 `-DPROJECT_ENABLE_TESTING=ON/OFF`、
`-DPROJECT_ENABLE_BENCHMARKS=ON/OFF`、`-DPROJECT_ENABLE_SPDLOG=ON/OFF`、
`-DPROJECT_ENABLE_FMT=ON/OFF` 和 `-DPROJECT_ENABLE_EIGEN=ON/OFF` 覆盖。
依赖版本会按照所选 C++ 标准自动选择，确保依赖自身也能用对应标准构建。

## 构建

需要 CMake 3.24+、支持所选 C++ 标准的编译器、Git 和 Ninja。
安装脚本已经执行过 Debug 配置，因此 `build/compile_commands.json` 可直接供
clangd 使用；修改依赖选项后需要重新运行 `cmake --preset debug`。

```bash
cmake --preset debug
cmake --build --preset debug
./build/debug/bin/@PROJECT_NAME@
```

## 测试

启用 Google Test 后：

```bash
ctest --preset debug
```

## Benchmark

启用 Google Benchmark 后，建议使用 Release 构建：

```bash
cmake --preset release
cmake --build --preset release --target project_benchmarks
./build/release/bin/@PROJECT_NAME@_benchmarks
```
